# 瀚邦为知识库助手交接记录

更新日期：2026-09-10  
对象：后续接手的开发、部署和文控同事  
说明：本文件记录会话中实际完成的工作及已知缺口，不把测试通过等同于全部业务验收完成。

## 1. 当前任务和目标

为公司全体员工提供飞书知识库问答机器人“瀚邦为知识库助手”。公司已有 Ubuntu 服务器和 DeepSeek API，采用自建 RAG 中间件，不依赖员工登录 Kimi。

用户要求：

- 全员可用、资料默认全员可读；当前重点是回答速度与准确性。
- 员工回复不展示来源区块、引用编号和内部片段信息。
- 支持财务、采购、行政人事、品质、IE 资料。
- 高频问题可预热成标准答案，资料更新时旧答案失效；普通高频统计窗口为 15 天。
- 不开发网页看板或美化界面。
- 常规开发和已授权部署连续执行，不反复要求逐项确认。
- 重要变更及时提交 Git，说明版本及改动；凭据不写入 Git 或员工材料。

本次最新要求是整理本 handoff.md。最近一项交付是桌面的员工 Word 操作说明，带版本、编写人和打开助手的操作示意图。

## 2. 位置和技术栈

| 项目 | 位置或说明 |
|---|---|
| 本地代码仓库 | `D:\瀚邦为工作\AI\知识库\RAGcode` |
| 部门原始资料 | `D:\瀚邦为工作\AI\知识库\各部门文档` |
| 会话工作目录 | `C:\Users\HBW\Documents\Codex\2026-08-27\new-chat` |
| GitHub | `https://github.com/LeonZhouForAi/Lark-Numbering-Middleware-.git` |
| Ubuntu 应用目录 | `/opt/data-assistant/feishu-rag` |
| 宿主机资料与索引 | 应用目录的 `documents/` 和 `data/rag.sqlite3` |
| 容器资料与索引 | `/app/documents` 和 `/app/data/rag.sqlite3` |
| 服务 | Docker Compose 的 `rag` 与 `rag-events` |
| 运行时 | 最后部署验证为 Python 3.14.7；本地 `.venv` 实测为 Python 3.12.14 |
| 问答与检索 | DeepSeek、SQLite FTS5/BM25、字面检索与 RRF；没有向量数据库 |
| 解析 | python-docx、openpyxl、pypdf、Poppler、Tesseract OCR |
| 飞书入口 | 自建机器人，通过长连接接收消息 |

SSH 地址、密码和 API Key 曾在会话中提供，不在此重复。使用服务器受控配置或向负责人取得凭据，不从聊天记录复制到仓库。现有 `.env` 必须保留，不用 `.env.example` 覆盖。

## 3. 已完成内容

### 开发与版本

- v0.6.0：XLSX 原生文本解析、文件名元数据提取、本地目录版本状态、旧版检索过滤、只读索引报告。
- v0.7.0：answerable、ambiguous、insufficient、missing 四类状态；单一澄清问题；答案格式；46 条评测用例与阈值参数。
- v0.8.0：预热候选筛选、作业队列、并行生成、FAQ 直接启用、资料缺口聚合及命令行报告。
- v0.9.0：IE UPPH、系列工时、料号工时结构化提取与精确查询；事实随文档事务更新；明确旧版事实被过滤。
- 能力介绍：询问“你可以做什么”“请问你会做什么”“你有什么用”等时直接返回部门范围和提问示例，不调用模型。

关键提交：

| 提交 | 内容 |
|---|---|
| d1c2e04 | v0.9.0 标签对应代码，排除旧版结构化事实 |
| dcc5277 | 能力介绍与员工提问指引 |
| 7451257 | 修正预热线程用量记录与全员范围匹配 |
| 445ba4c | 按当前资料版本自动入队全员预热 |
| c96a56e | 生产部署记录 |
| eb716d3 | 补齐“会做什么”等问法，最后一次代码部署 |

2026-09-10 本地只读核对：main 与 origin/main 同步，HEAD 为 eb716d3，新增本文件前未发现 Git 状态列出的变更。Git 对 `.pytest_tmp` 和全局 ignore 存在访问警告，因此不能据此断言所有不可访问目录均为空。

### 部署与验证

2026-09-07 已从线上 v0.2.3 升级到 v0.9.0 加部署修复，并推送 main 和 v0.6.0 至 v0.9.0 标签。

最后部署镜像：`hbw-rag:eb716d3`，对应 Compose 使用的本地镜像标签 `feishu-rag-rag`、`feishu-rag-rag-events` 已更新。

- 新镜像先在数据库副本完成迁移与索引，之后切换生产。
- 服务器 Python 3.14 全量测试运行到 100%，输出没有失败项；后续小修复执行了对应测试。
- 上次统计：49 份文档、347 个片段、2605 条结构化事实。该数字是 9 月 7 日快照，不是 9 月 10 日实时统计。
- 实际核对：Cell AOI2 线开机为 450；氧化物系列绑定工时为 101；055010D 合计工时为 1026。
- 真实 DeepSeek 冒烟：费用报销、供应商开发、品质异常均返回 answerable，回复不含来源区块。
- 健康端点正常，飞书长连接已连接；同步和预热定时器均 active。
- 首轮预热 10 个候选，9 个成功，1 个失败；失败候选不启用。
- 能力介绍修复后，在运行中的 rag-events 容器验证截图原句“请问你会做什么”成功。
- 尚未收到修复后员工手机实际收发成功的反馈；不能写成手机端已验收。

### 文档与资料接入

- 品质部 15 份 DOCX 已复制到服务器 `documents/quality/`。旧解析器曾只索引 13 份，另两份为纯表格；新版具备表格解析，仍建议逐份核对生产索引清单。
- IE 目录有两份 XLSX 和一份 `提案改善管理办法20260615.docx`，已复制到服务器 `documents/ie/`。
- IE 初期还生成了两份 Markdown 副本；新版原生索引 XLSX 后，尚未确认副本已清理，可能重复检索。
- 品质和 IE 均有同名提案改善文档，需检查内容哈希并去重。
- 飞书“品质管理知识库”曾异步创建出两个同名空间；空白重复空间已删除，保留 ID `7681574666615475447`，首页 token `FmNEwBELgiRKi6kB7AzcO6huneh`。
- 品质 15 份文件上传飞书的操作未取得成功证据；不能把服务器上传完成当成飞书云端上传完成。
- “IE工艺效率知识库”发起过异步创建，当时列表未出现。创建是否后来完成、3 份 IE 文件是否上传飞书尚未核对；不要直接再次创建。

### 员工培训资料

- 桌面初稿：`瀚邦为知识库助手使用说明.docx`。
- 桌面受控版本：`瀚邦为知识库助手使用说明_v1.0.0.docx`。
- 编写人周安阳，日期 2026-09-07，文档编号 HBW-KB-USER-001。
- 包含登录飞书、搜索助手、进入聊天的三步操作示意图；明确标注为示意图，不是实际界面截图。
- 包含适用对象、提问示例、异常处理、版本规则和修订记录；文档版本独立于软件版本。
- 内容、图片嵌入和元数据已核对；Word 页面未完成渲染检查。旧版文件仍保留。
- 生成脚本：会话目录 `build_employee_guide_v1.py`；图：`employee-guide-opening.png`。

## 4. 卡住的问题与尚未完成的验收

1. **Word 排版验证缺少渲染器。** 文档技能的 render_docx.py 报 `LibreOffice soffice.exe was not found on PATH`；捆绑运行时未找到 LibreOffice。已交付可读取 DOCX，但不应声称页面视觉验收完成。
2. **飞书云端资料未闭环。** 品质/IE 的空间状态、上传数量和应用读取权限需重新检查；旧同步 service 明确列出的仍是财务、采购、行政三个空间。
3. **预热 1 条失败原因未查清。** 目前只看到 generated=9、failed=1，未逐候选归因。
4. **日志有一次 EventException。** 9 月 7 日能力介绍修复重启后，rag-events 出现一次 `message_handler_failed error_type=EventException`，长连接随后检查正常，但该异常未深入定位。
5. **业务评测未完整执行。** 46 条题库加载和评测代码测试通过，不等于 46 道真实业务题全部答对。部分期望标题/来源 ID 是早期示例，与实际索引标题不一致，需校准后再用于发布门禁。
6. **先前“全部完成”措辞过强。** 开发测试和标签已经完成，但批准规格中的正文版本识别、失败报告、全范围自动更新等仍需逐项对照代码验收。

## 5. 接手后优先检查的代码边界

以下是从当前实现和执行记录可见的风险点，应通过测试确认后修复，不能直接视为已经修好：

- `document_metadata.py` 目前主要读文件名；文件名日期不能无条件等同于制度生效日期，尚未实现完整正文元数据识别。
- `sync.py` 明确把飞书附件格式与本地格式分开；飞书 XLSX 附件目前不在同步支持范围内。
- 本地原生 XLSX 已支持，但 Markdown 副本与原件一起存在时不会自动识别为同一文件。
- `exact_query.py` 自动精确查询路径未接收 RetrievalScope，也在普通 RAG 版本检查循环之前；当前全员配置可用，但范围过滤和并发版本更新仍需回归。
- `structured_facts.py` 的单位大多为空，UPH/UPPH 存在统一命名行为，需和 IE 确认口径，不能擅自赋予小时、秒或人均定义。
- 同一实体重复或冲突事实、未知工序和多条件问题的澄清/回退行为需实测。
- `preheat.py` 的别名必须归一化成同一意图键才保留；来源重叠阈值较高，多文档召回可能导致预热有答案但无法直接命中。
- 全员预热使用 global 作业，每轮默认最多 10 条，不等于每个部门各 10 条。
- 预热入队当前在快照提交之后，并不是与索引同一事务；相关 CHANGELOG 的“原子入队”描述需校正。
- 预热租约超时重新排队未完整消耗重试计数，完成操作也缺少租约令牌隔离；多个过期 worker 可能交叉完成任务。
- `question_gaps` 目前主要做字符规范化，并未完整复用 FAQ 同义归一化；报告默认 count>=2 作用于所有缺口类型。
- 模型的澄清文本目前直接返回，和普通答案的清洗处理不完全一致。
- 修改结构化提取规则后应提升解析器签名，防止“文件未变化”导致不补建事实。

## 6. 下一步计划

1. 只读检查线上镜像、健康状态、定时器、索引数量和近期错误；用员工实际问法做收发验收。
2. 校准真实业务题库，并优先修复上节会造成错误回答或标准答案失效的问题；按小提交测试、推送、部署，不移动既有标签。
3. 查清预热失败项，验证常用问题首次直发和资料更新后旧答案失效。
4. 清点品质/IE 飞书空间和文件，避免重复创建；完成云端上传及同步范围配置。
5. 比较服务器 IE Markdown 副本、原始 XLSX 和重复 Word 文档，按已授权的旧版清理要求精确去重；先检查目标，不做根目录清空。
6. 完成员工操作说明的页面渲染或实际 Word/WPS 检查。文字排版修订用 v1.0.1；增加实际操作截图等内容用 v1.1.0。更新修订记录和桌面文件名。
7. 更新本文件和部署记录，标注真实验收结果，不只记录“测试 100%”。

## 7. 踩过的坑

- **异步创建不能立即重试。** 飞书创建后当前页可能不跳转、列表暂未刷新，曾误判失败而创建两个空间。先检查新标签页、异步创建页、列表及空间 ID。
- **SSH 交互每次只发一条命令。** 密码提示未结束时批量输入会把命令当密码；连续命令也曾被终端吃掉开头字符。等待提示符再继续。
- **保留运行会话 ID。** exec 的 output 不代表完成；必须保留 session_id，再等待退出码。构建建议 `--progress=plain` 写日志后读摘要，避免动态终端输出膨胀。
- **SQLite 文件权限。** 新容器以 UID/GID 10001 运行；生产 data 旧所有权造成只读数据库错误。已对应用 data 目录修正，不能把权限修复扩展到其他路径。
- **SQLite 不能跨线程共用连接。** 预热线程的 Token 用量记录改为每次创建独立连接并关闭。
- **本地 Python 版本并非声明版本。** pyproject/Docker 为 3.14，但本地 `.venv` 实测 3.12.14；后来在服务器 3.14.7 镜像中完成全量测试。
- **依赖安装卡住。** 本地 pip 安装 openpyxl 多次无进度，测试曾临时复用捆绑 openpyxl；不要将整个捆绑 site-packages 加到路径中长期覆盖项目依赖。
- **临时目录 ACL。** 默认 pytest 临时目录会被不同隔离账户锁住。使用会话目录内唯一的 `--basetemp` 和 `-p no:cacheprovider`。
- **PowerShell 引号。** 反斜杠不是 PowerShell 的双引号转义；嵌套 SQL/Python 曾意外解析失败。复杂逻辑先写脚本，再传路径执行。
- **共享格式常量。** 本地加入 XLSX 会同时改变导入同一常量的飞书同步行为，曾导致大量回归；现已拆分。
- **测试结果不等于整体验收。** 文件名匹配、表头识别、单位、实际资料齐全与否需要真实样本检查。
- **IE 真实表头是“类别”。** 料号表原先按“系列”判断，导致漏提取；已兼容两个名字。
- **能力介绍问法。** 最初漏掉“请问你会做什么”，员工截图证明普通检索兜底了；eb716d3 已补并部署。
- **不要无条件串联测试和提交。** PowerShell 分号不会在测试失败时停止；曾发生测试因临时目录权限失败而后续提交仍执行。应检查退出码后提交。
- **工作树删除。** 已合并分支的残留目录含 .venv/data/documents，不能默认只有代码。删除曾被审核及 ACL 拦截，用户明确确认并最终手工删除；2026-09-05 已验证目录不存在。
- **不要再次删除其他工作树。** `v0.2.4-hardening`、`v0.5.1-faq-preheat` 曾仍存在，内容可能不同，需单独核对，不在本次清理范围。

## 8. 回滚与关键交接文件

- 生产备份目录：`/opt/data-assistant/rag-backups/20260907-7451257`。
- `code-config.tar.gz`：旧代码和配置；`rag.sqlite3`：SQLite 在线一致性备份；`final-rag.sqlite3`：停机后副本。
- 旧镜像：`hbw-rag:rollback-20260907`、`hbw-rag-events:rollback-20260907`。
- 恢复 v0.2.3 必须恢复对应数据库，不能让旧程序直接操作已迁移的库。
- `docs/operations/2026-09-07-production-deployment.md` 记录初次部署；其中 445ba4c 镜像已被后续 eb716d3 替代，阅读时以本文件更新说明为准。
- 总规格：`docs/superpowers/specs/2026-09-04-v0.6-v0.9-knowledge-quality-roadmap-design.md`。
- 各版本计划：`docs/superpowers/plans/2026-09-04-v0.*.md`。部分复选框未回填，不能单凭勾选判断实现状态。
- 已删除工作树的未跟踪锁文件备份：`C:\Users\HBW\Documents\Codex\2026-08-27\new-chat\preserved-v0.9-uv.lock`。

本文件不包含服务器密码、DeepSeek Key 或飞书 App Secret。交接时通过受控渠道传递运行凭据。
