# 版本说明

本文档记录“瀚邦为知识库助手”RAG 中间件的重要版本变化。

## 未发布

暂无。

## 0.5.1 - 2026-09-01

### 高频问答修复

- FAQ 入库前过滤 PII，不保存员工身份；仅保留经过过滤的归一化意图、安全答案和资料特征。
- 修复全路径资料版本变更的重试处理，确保资料更新后的 FAQ 失效与首问刷新覆盖完整路径。
- 修复本地索引更新的原子清理边界，失败时保留旧索引，成功时再清理失效资料。
- 将 `report_faq.py` 限制为只读统计操作，不修改 FAQ 或指标数据。
- 修复 FAQ 指标的聚合与直接命中统计，确保报表和日志不输出员工身份、问题、答案或来源。

## 0.5.0 - 2026-09-01

### 高频问答索引

- 增加高频 FAQ 缓存：默认在最近 15 天内累计 3 次安全回答后晋级，第 4 次起可直接回复；阈值和窗口均可配置。
- 使用本地问题同义归一化与资料来源特征匹配，不引入向量数据库。
- 资料版本原子更新后自动使旧缓存失效，并在资料更新后的首个安全回答中刷新缓存。
- 增加匿名 FAQ 统计报表与 CLI 清理命令；FAQ 存储经过 PII 过滤的归一化意图、答案和资料特征，报表/日志不输出问题、答案、来源或员工身份信息。

## 0.4.0 - 2026-08-31

### 检索质量与费用治理

- 使用 SQLite FTS5/BM25 生成候选，并与字面检索混排；以 confidence 和 `RAG_MIN_RELEVANCE=0.42` 过滤泛词及无关命中。
- 回答阶段使用严格的匿名结构化 JSON，仅把原文作为上下文；证据不足、来源泄漏或危险输出均 fail-closed。
- 飞书幂等读取请求支持有限重试；新增按模型、用途和日期聚合的 Token usage report。
- 按用户哈希执行分钟/日限流，检索支持 `RetrievalScope` 空间边界，为后续 ACL 接入保留 seam。
- 长连接使用有界工作线程，并在完整处理后才返回 ACK；处理失败返回非成功状态以便飞书重投，同时保持 WebSocket 心跳不被慢模型调用阻塞。
- 消息租约增加 UUID fencing token，并以 `in_progress -> replying` CAS 原子封口；封口后禁止接管，崩溃时按 at-most-once 处理，旧工作器和无 token 调用不能完成、释放或发送新租约的答复。
- 增加 v0.4 到 v0.3 的安全回滚 CLI：只读预检后显式执行 SQLite 在线备份，再在事务内移除不兼容的 FTS v2 表；禁止直接用 v0.3 写入 v0.4 FTS。
- DeepSeek Chat Completions 生成 POST 改为单次尝试；Token usage 仅统计成功响应，网络结果不明时的费用以服务商账单为准。
- 输出脱敏补充密码、口令、密钥、API 密钥、访问令牌和令牌等中文标签，并保留正常制度说明的误杀回归测试。
- `lark-oapi` 精确锁定到已测试的 `1.7.3`，新增真实私有 ACK 合约测试，SDK 升级必须显式验证。
- 保持 `hybrid-v4` 切片策略；本版本不引入向量数据库，代码与测试已完成但尚未部署生产环境。

## 0.3.0 - 2026-08-30

### 索引生命周期与质量门禁

- 只有完整成功快照才清理失效的飞书文档索引，分页或同步失败时保留旧数据。
- 同时提取父页面正文、DOCX 表格/嵌套可见文本，并对混合 PDF 按页 OCR。
- 增加不含制度正文的金标 CLI 与脱敏质量报告，支持检索命中率、MRR 和泄漏门禁。
- 切片策略版本升级到 `hybrid-v4`，强制现有附件重新解析。
- 本版本已完成开发与测试，尚未部署生产环境。

## 0.2.4 - 2026-08-30

### 稳定性与安全

- 修复长文混合切片边界，确保原文字符不丢失。
- 默认拒绝未配置密钥的 Webhook 请求，并默认绑定本地地址。
- SQLite 启用 WAL 模式并设置 30 秒忙等待，降低并发访问冲突。
- 日志脱敏，避免密钥等敏感配置进入日志。
- 本地索引遵循当前切片策略版本，避免复用旧策略缓存。

## 0.2.3 - 2026-08-30

### 运行时升级

- 运行时统一到 Python 3.14：Docker 基础镜像、项目版本约束和部署文档保持一致。

## 0.2.2 - 2026-08-30

### 缺陷修复

- 提升混合切片策略版本，强制重新处理此前因 DeepSeek 思考输出导致回退的长文。
- 确保关闭思考模式的修复覆盖既有生产索引，而不是被旧校验和缓存跳过。

## 0.2.1 - 2026-08-30

### 缺陷修复

- 修复 DeepSeek V4-Flash 语义切片请求因思考输出占满额度、正文为空而回退本地切片的问题。
- 语义分组请求明确使用非思考模式，保留最终问答的原有模型设置。

## 0.2.0 - 2026-08-30

### 新功能

- 增加本地结构切分与 DeepSeek 语义分组的混合切片流程。
- DeepSeek 只返回段落分组、标题、关键词和摘要，制度正文由原文重组。
- 增加 JSON 输出校验、策略签名缓存和本地切片回退。
- 员工回答隐藏来源区块和引用编号，后台仍保留引用数据。
- 增加三部门固定问题验收脚本。

### 兼容性

- SQLite 启动时自动增加 `search_text` 字段，旧数据库无需手工迁移。
- 可设置 `RAG_SEMANTIC_CHUNKING=false` 关闭 DeepSeek 语义切片。

## 0.1.0 - 2026-08-30

### 新功能

- 新建独立飞书机器人，面向公司所有员工使用。
- 部署 Ubuntu RAG 中间件和飞书长连接服务。
- 接入 DeepSeek API 生成制度问答。
- 支持 PDF、DOCX、TXT、Markdown 和扫描 PDF 中文 OCR。
- 递归同步财务、采购、行政人事三个飞书知识库。
- 每小时自动下载并刷新飞书附件索引。

### 稳定性

- 使用 SQLite 持久化消息 ID，阻止飞书重复投递导致重复回复。
- 文档未变化时跳过重复 OCR。
- DeepSeek 或同步异常时保留可诊断错误信息且不泄露密钥。

### 安全

- API Key、App Secret、服务器密码、数据库和公司文档不进入 Git。
- 服务使用非 root 容器用户和只读容器文件系统。
- 机器人不允许加入外部群或与外部用户单聊。
