# v0.2.4 正确性与安全加固实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 发布 v0.2.4 热修，保证本地回退切片不丢正文，收紧 Webhook/端口，增强 SQLite 并发保护，并让语义降级可诊断。

**架构：** 保持现有两容器和 SQLite 架构不变。只在切片合并、安全边界、数据库连接初始化和日志入口做局部修改；用现有配置扩展 `RAG_BIND_HOST` 与 `LOG_LEVEL`，不新增第三方依赖。

**技术栈：** Python 3.14、pytest、SQLite、FastAPI、Docker Compose、Python logging。

---

## 文件结构

- 修改 `src/feishu_rag/chunker.py`：移除会截断正文的 overlap 合并逻辑。
- 修改 `tests/test_chunker.py`：增加字符完整覆盖回归测试。
- 修改 `src/feishu_rag/web.py`：空 Encrypt Key 时拒绝 Webhook 签名。
- 修改 `tests/test_webhook.py`：增加空密钥拒绝测试。
- 修改 `docker-compose.yml`、`.env.example`：默认端口只绑定本机。
- 修改 `src/feishu_rag/store.py`：设置 WAL、busy timeout 和 NORMAL。
- 修改 `tests/test_store.py`：验证数据库并发 PRAGMA。
- 创建 `src/feishu_rag/logging_utils.py`：应用现有日志级别配置。
- 修改 `src/feishu_rag/sync.py`、`src/feishu_rag/ingest.py`、`src/feishu_rag/long_connection.py`：记录降级、同步结果和回调异常。
- 修改对应测试：验证日志行为和回调异常不逸出。
- 修改 `pyproject.toml`、`src/feishu_rag/config.py`、`.env.example`、`README.md`、`CHANGELOG.md`：升级版本和策略。

### 任务 1：修复本地切片丢字

**文件：**
- 修改：`tests/test_chunker.py`
- 修改：`src/feishu_rag/chunker.py:53-63`

- [x] **步骤 1：编写失败的覆盖率测试**

```python
def test_long_text_overlap_never_drops_original_characters():
    text = "".join(chr(0x4E00 + index) for index in range(100))
    chunks = chunk_text(text, "source", "title", max_chars=30, overlap=5)
    indexed = "".join(chunk.content for chunk in chunks)
    assert all(character in indexed for character in text)
    assert all(len(chunk.content) <= 30 for chunk in chunks)
```

- [x] **步骤 2：运行测试确认旧实现失败**

运行：`python -m pytest tests/test_chunker.py::test_long_text_overlap_never_drops_original_characters -q`

预期：FAIL，至少有一个原文字符不在切片中。

- [x] **步骤 3：最小修复 overlap 合并**

在 `chunk_text` 中先计算当前 `unit` 占用，只有剩余容量足够时才加入上一片尾部；不得截断 `unit`：

```python
separator = "\n\n" if carry else ""
available = max_chars - len(unit) - len(separator)
carry = carry[-available:] if available > 0 else ""
current = f"{carry}{separator}{unit}" if carry else unit
```

- [x] **步骤 4：运行目标测试和全部切片测试**

运行：`python -m pytest tests/test_chunker.py -q`

预期：全部通过。

- [ ] **步骤 5：提交**

```bash
git add src/feishu_rag/chunker.py tests/test_chunker.py
git commit -m "fix(切片): 保证本地回退不丢失原文"
```

### 任务 2：收紧 Webhook 和端口

**文件：**
- 修改：`tests/test_webhook.py`
- 修改：`src/feishu_rag/web.py:24-28`
- 修改：`docker-compose.yml:6-7`
- 修改：`.env.example`

- [x] **步骤 1：编写空密钥拒绝测试**

```python
def test_signature_rejects_when_encrypt_key_is_missing(self):
    self.assertFalse(verify_signature("1", "nonce", "{}", "", ""))
```

- [x] **步骤 2：运行测试确认失败**

运行：`python -m pytest tests/test_webhook.py::WebhookTests::test_signature_rejects_when_encrypt_key_is_missing -q`

预期：FAIL，旧实现返回 True。

- [x] **步骤 3：最小修改签名与端口配置**

```python
if not encrypt_key:
    return False
```

Compose 端口改为：

```yaml
- "${RAG_BIND_HOST:-127.0.0.1}:${RAG_HOST_PORT:-8010}:8000"
```

- [x] **步骤 4：运行 Webhook 测试和 Compose 配置检查**

运行：`python -m pytest tests/test_webhook.py -q`

运行：`docker compose config --quiet`

预期：均成功。

- [ ] **步骤 5：提交**

```bash
git add src/feishu_rag/web.py tests/test_webhook.py docker-compose.yml .env.example
git commit -m "fix(安全): 默认拒绝未签名 Webhook"
```

### 任务 3：增加 SQLite 并发保护

**文件：**
- 修改：`tests/test_store.py`
- 修改：`src/feishu_rag/store.py:17-24`

- [x] **步骤 1：编写 PRAGMA 失败测试**

```python
def test_store_enables_concurrency_pragmas(self):
    store = IndexStore(tmp_path / "rag.sqlite3")
    assert store.connection.execute("PRAGMA journal_mode").fetchone()[0] == "wal"
    assert store.connection.execute("PRAGMA busy_timeout").fetchone()[0] >= 30000
    assert store.connection.execute("PRAGMA synchronous").fetchone()[0] == 1
```

- [x] **步骤 2：运行测试确认失败**

运行：`python -m pytest tests/test_store.py -q`

预期：FAIL，旧连接未启用 WAL 或 busy timeout 不足 30000。

- [x] **步骤 3：设置数据库连接参数**

```python
self.connection = sqlite3.connect(self.db_path, timeout=30.0)
self.connection.execute("PRAGMA journal_mode = WAL")
self.connection.execute("PRAGMA busy_timeout = 30000")
self.connection.execute("PRAGMA synchronous = NORMAL")
self.connection.execute("PRAGMA foreign_keys = ON")
```

- [x] **步骤 4：运行 Store 测试**

运行：`python -m pytest tests/test_store.py -q`

预期：全部通过。

- [ ] **步骤 5：提交**

```bash
git add src/feishu_rag/store.py tests/test_store.py
git commit -m "fix(数据库): 增强 SQLite 并发读写保护"
```

### 任务 4：增加安全日志

**文件：**
- 创建：`src/feishu_rag/logging_utils.py`
- 修改：`src/feishu_rag/sync.py`
- 修改：`src/feishu_rag/ingest.py`
- 修改：`src/feishu_rag/long_connection.py`
- 修改：`tests/test_feishu_sync.py`
- 修改：`tests/test_long_connection.py`

- [x] **步骤 1：编写语义回退日志测试**

使用 `assertLogs("feishu_rag.sync", level="WARNING")` 调用失败规划器，断言日志包含 `semantic_chunk_fallback` 和异常类型，但不包含正文。

- [x] **步骤 2：编写长连接异常隔离测试**

构造会抛异常的 `FakeRag`，调用新的回调辅助函数，断言异常不会逸出且日志包含 `message_handler_failed`。

- [x] **步骤 3：运行测试确认失败**

运行：`python -m pytest tests/test_feishu_sync.py tests/test_long_connection.py -q`

预期：FAIL，旧实现没有日志或回调隔离函数。

- [x] **步骤 4：实现最小日志配置与记录**

`logging_utils.configure_logging(level)` 使用 `logging.basicConfig`。同步和本地索引只捕获现有异常后记录异常类型；长连接将消息处理放入可测试的 `_safe_handle_message`，记录错误后继续监听。

- [x] **步骤 5：运行目标测试**

运行：`python -m pytest tests/test_feishu_sync.py tests/test_long_connection.py -q`

预期：全部通过，日志不包含正文和密钥。

- [ ] **步骤 6：提交**

```bash
git add src/feishu_rag/logging_utils.py src/feishu_rag/sync.py src/feishu_rag/ingest.py src/feishu_rag/long_connection.py tests/test_feishu_sync.py tests/test_long_connection.py
git commit -m "feat(日志): 记录语义降级和消息处理异常"
```

### 任务 5：升级版本并完整验收

**文件：**
- 修改：`pyproject.toml`
- 修改：`src/feishu_rag/config.py`
- 修改：`.env.example`
- 修改：`README.md`
- 修改：`CHANGELOG.md`

- [x] **步骤 1：更新版本和策略**

- `version = "0.2.4"`
- 默认 `RAG_CHUNK_STRATEGY_VERSION=hybrid-v3`
- README 修正旧的 `hybrid-v1` 示例，并说明默认绑定本机。
- CHANGELOG 增加 v0.2.4 正确性、安全、数据库和日志说明。

- [x] **步骤 2：运行完整测试**

运行：`python -m pytest -q`

预期：全部测试通过。

- [x] **步骤 3：运行静态与安全检查**

```bash
python -m compileall -q src
git diff --check
git grep -nE 'sk-[A-Za-z0-9]{20,}|FEISHU_APP_SECRET=.{8,}|hbw@'
```

预期：编译和 diff 检查成功，密钥扫描无命中。

- [ ] **步骤 4：提交版本**

```bash
git add pyproject.toml src/feishu_rag/config.py .env.example README.md CHANGELOG.md docs/superpowers/plans/2026-08-30-v0.2.4-hardening.md
git commit -m "chore(版本): 发布 v0.2.4 正确性与安全热修"
```

- [ ] **步骤 5：检查提交和工作区**

运行：`git status --short --branch && git log --oneline -8`

预期：工作区干净，分支包含设计、4 个实现提交和版本提交。
