# v0.6.0 资料解析与版本治理实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 让本地索引原生读取 XLSX，并为所有本地文档保存可靠的编号、版本和生命周期；明确旧版不参与检索，版本冲突文件保持可检索并进入索引报告。

**架构：** `document_metadata.py` 只负责从标题提取和比较元数据；`xlsx_reader.py` 只负责把工作簿转换为按工作表分组的 `Section`。`ingest.py` 组合解析器与版本决策，`store.py` 事务性保存元数据、版本关系和片段，并在搜索层排除 `superseded`。整个目录先在内存中准备成功，再通过现有 `apply_document_snapshot()` 一次提交，因此任一新文件失败时旧快照保持不变。

**技术栈：** Python 3.14、openpyxl、python-docx、pypdf、SQLite/FTS5、pytest。

---

## 文件结构

- 创建：`src/feishu_rag/document_metadata.py`——文件编号、版本、日期提取和生命周期判定。
- 创建：`src/feishu_rag/xlsx_reader.py`——XLSX 工作表读取、合并单元格展开和检索文本生成。
- 创建：`scripts/report_indexing.py`——只读输出当前、旧版、冲突和解析失败统计。
- 创建：`tests/test_document_metadata.py`——元数据和版本冲突行为测试。
- 创建：`tests/test_xlsx_reader.py`——XLSX 解析和合并单元格测试。
- 创建：`tests/test_report_indexing.py`——索引报告只读行为测试。
- 修改：`src/feishu_rag/ingest.py`——注册 XLSX、解析器版本签名、目录级版本决策。
- 修改：`src/feishu_rag/store.py`——迁移文档元数据、保存版本关系、检索生命周期过滤。
- 修改：`tests/test_ingest.py`——XLSX 目录索引、失败回滚和解析器版本缓存测试。
- 修改：`tests/test_store.py`——旧库迁移、元数据更新、版本过滤和事务测试。
- 修改：`scripts/index_local_documents.py`——输出生命周期统计。
- 修改：`pyproject.toml`——加入 openpyxl 运行依赖并升级到 0.6.0。
- 修改：`README.md`、`CHANGELOG.md`、`.env.example`——记录 XLSX、版本规则、命令与发布说明。

### 任务 1：文档元数据提取与版本判定

**文件：**
- 创建：`src/feishu_rag/document_metadata.py`
- 创建：`tests/test_document_metadata.py`

- [ ] **步骤 1：编写文件编号、版本和日期提取的失败测试**

```python
from feishu_rag.document_metadata import extract_document_metadata


def test_extracts_company_code_and_revision_from_filename() -> None:
    metadata = extract_document_metadata("HBW-PZ-LCM-WI-002 品质异常处理规范A5.docx")
    assert metadata.document_code == "HBW-PZ-LCM-WI-002"
    assert metadata.document_version == "A5"
    assert metadata.effective_date is None


def test_extracts_compact_date_without_inventing_document_code() -> None:
    metadata = extract_document_metadata("提案改善管理办法20260615.docx")
    assert metadata.document_code is None
    assert metadata.document_version is None
    assert metadata.effective_date == "2026-06-15"
```

- [ ] **步骤 2：运行测试并确认因模块不存在而失败**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_document_metadata.py -q`

预期：FAIL，包含 `ModuleNotFoundError: No module named 'feishu_rag.document_metadata'`。

- [ ] **步骤 3：实现最小元数据模型和提取函数**

```python
@dataclass(frozen=True)
class DocumentMetadata:
    document_code: str | None = None
    document_version: str | None = None
    effective_date: str | None = None
    lifecycle_state: str = "current"
    decision_reason: str = "unique-or-unversioned"


def extract_document_metadata(filename: str) -> DocumentMetadata:
    stem = Path(filename).stem
    code_match = _DOCUMENT_CODE_RE.search(stem)
    version_match = _VERSION_RE.search(stem)
    date_match = _COMPACT_DATE_RE.search(stem)
    return DocumentMetadata(
        document_code=code_match.group(0).upper() if code_match else None,
        document_version=version_match.group(1).upper() if version_match else None,
        effective_date=_iso_date(date_match.group(1)) if date_match else None,
    )
```

版本正则必须只在编号或标题末尾接受 `A0`、`B1` 等形式，不能把 `AOI1`、`IPQC` 或日期的一部分识别为版本。

- [ ] **步骤 4：编写明确旧版和冲突版本的失败测试**

```python
from feishu_rag.document_metadata import DocumentMetadata, resolve_lifecycle_states


def test_resolver_marks_only_lower_revision_as_superseded() -> None:
    resolved = resolve_lifecycle_states({
        "a.docx": DocumentMetadata("HBW-OP-022", "A1"),
        "b.docx": DocumentMetadata("HBW-OP-022", "B0"),
    })
    assert resolved["a.docx"].lifecycle_state == "superseded"
    assert resolved["b.docx"].lifecycle_state == "current"


def test_resolver_keeps_uncomparable_versions_as_conflict() -> None:
    resolved = resolve_lifecycle_states({
        "a.docx": DocumentMetadata("HBW-OP-022", None),
        "b.docx": DocumentMetadata("HBW-OP-022", "B0"),
    })
    assert {item.lifecycle_state for item in resolved.values()} == {"conflict"}
```

- [ ] **步骤 5：运行新增测试并确认版本判定尚未实现**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_document_metadata.py -q`

预期：至少版本判定测试 FAIL，原因是 `resolve_lifecycle_states` 不存在或返回错误状态。

- [ ] **步骤 6：实现可解释的版本排序和冲突处理**

```python
def revision_key(value: str) -> tuple[int, int] | None:
    match = re.fullmatch(r"([A-Z])(\d+)", value.strip().upper())
    if match is None:
        return None
    return ord(match.group(1)) - ord("A"), int(match.group(2))


def resolve_lifecycle_states(
    documents: Mapping[str, DocumentMetadata],
) -> dict[str, DocumentMetadata]:
    # 无编号或编号唯一：current，reason=unique-or-unversioned。
    # 同编号全部可比较：最高版本 current，其余 superseded，reason=highest-revision/lower-revision。
    # 同编号存在缺失、重复最高版本或不可比较值：该组全部 conflict，reason=uncomparable-revisions。
```

- [ ] **步骤 7：运行元数据测试并确认通过**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_document_metadata.py -q`

预期：PASS。

- [ ] **步骤 8：提交元数据模块**

```text
git add src/feishu_rag/document_metadata.py tests/test_document_metadata.py
git commit -m "feat(索引): 增加文档版本元数据判定"
```

### 任务 2：XLSX 原生解析

**文件：**
- 创建：`src/feishu_rag/xlsx_reader.py`
- 创建：`tests/test_xlsx_reader.py`
- 修改：`pyproject.toml`

- [ ] **步骤 1：添加 openpyxl 运行依赖**

在 `dependencies` 中加入：

```toml
"openpyxl>=3.1,<4",
```

- [ ] **步骤 2：编写工作表、空行、数值和合并单元格的失败测试**

```python
from openpyxl import Workbook
from feishu_rag.xlsx_reader import read_xlsx_sections


def test_xlsx_rows_are_searchable_and_keep_sheet_name(tmp_path) -> None:
    path = tmp_path / "各岗位标准UPPH.xlsx"
    workbook = Workbook()
    sheet = workbook.active
    sheet.title = "岗位标准"
    sheet.append(["序号", "工段", "工序", "UPH"])
    sheet.append([1, "切割", "Cell AOI2线开机", 450])
    sheet.append([None, None, None, None])
    workbook.save(path)

    sections = read_xlsx_sections(path)

    assert len(sections) == 1
    assert sections[0].section == "岗位标准"
    assert "工序: Cell AOI2线开机" in sections[0].text
    assert "UPH: 450" in sections[0].text


def test_merged_header_value_is_available_to_each_data_row(tmp_path) -> None:
    path = tmp_path / "工时.xlsx"
    workbook = Workbook()
    sheet = workbook.active
    sheet.merge_cells("A1:A2")
    sheet["A1"] = "工段"
    sheet["B1"] = "常规系列"
    sheet["B2"] = "工时"
    sheet.append(["切割", 99])
    workbook.save(path)

    text = "\n".join(section.text for section in read_xlsx_sections(path))
    assert "工段: 切割" in text
    assert "常规系列 / 工时: 99" in text
```

- [ ] **步骤 3：运行测试并确认因 XLSX 读取器不存在而失败**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_xlsx_reader.py -q`

预期：FAIL，包含模块导入错误。

- [ ] **步骤 4：实现只读工作簿解析器**

`read_xlsx_sections(path)` 必须：

1. 以 `data_only=True` 读取公式缓存值。
2. 忽略完全空白工作表和完全空白行。
3. 将合并区域左上角值投影到合并范围用于表头构建，但不复制成虚假数据行。
4. 从开头最多三行构建非空表头，组合表头使用 ` / `。
5. 每个数据行转换为 `字段: 值`，数值使用 Excel 实际值，不加千位格式。
6. 每个工作表返回一个或多个 `Section`，`section` 等于工作表名称。
7. 工作簿损坏或没有任何有效数据时抛出 `DocumentExtractionError`，不能返回成功的空结果。

公开接口：

```python
def read_xlsx_sections(path: Path) -> list[SectionData]:
    ...
```

为避免循环导入，`xlsx_reader.py` 定义内部 `XlsxSection`；`ingest.py` 再转换为现有 `Section`。

- [ ] **步骤 5：运行 XLSX 测试并确认通过**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_xlsx_reader.py -q`

预期：PASS。

- [ ] **步骤 6：提交 XLSX 读取器**

```text
git add pyproject.toml src/feishu_rag/xlsx_reader.py tests/test_xlsx_reader.py
git commit -m "feat(索引): 原生解析XLSX工作表"
```

### 任务 3：把 XLSX 和解析器版本接入索引

**文件：**
- 修改：`src/feishu_rag/ingest.py`
- 修改：`tests/test_ingest.py`
- 修改：`scripts/index_local_documents.py`

- [ ] **步骤 1：编写 XLSX 目录索引的失败测试**

```python
def test_indexes_xlsx_without_markdown_sidecar(tmp_path) -> None:
    workbook = Workbook()
    sheet = workbook.active
    sheet.append(["工序", "UPH"])
    sheet.append(["Cell AOI2线开机", 450])
    path = tmp_path / "各岗位标准UPPH.xlsx"
    workbook.save(path)
    store = IndexStore(tmp_path / "index.sqlite3")
    try:
        assert index_directory(tmp_path, store) == 1
        assert store.search("AOI2开机UPH")[0].chunk.title == "各岗位标准UPPH"
    finally:
        store.close()
```

- [ ] **步骤 2：编写解析器版本变化触发重建的失败测试**

```python
def test_parser_version_change_reindexes_same_file(tmp_path) -> None:
    path = tmp_path / "制度.txt"
    path.write_text("制度正文", encoding="utf-8")
    store = IndexStore(tmp_path / "index.sqlite3")
    try:
        assert index_file(path, tmp_path, store, parser_version="parser-v1")
        assert not index_file(path, tmp_path, store, parser_version="parser-v1")
        assert index_file(path, tmp_path, store, parser_version="parser-v2")
    finally:
        store.close()
```

- [ ] **步骤 3：运行两个测试并确认功能缺失导致失败**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_ingest.py -q -k "xlsx_without or parser_version"`

预期：FAIL；XLSX 被跳过，且 `index_file` 不接受 `parser_version`。

- [ ] **步骤 4：接入 XLSX 和解析器签名**

修改内容：

- `SUPPORTED_SUFFIXES` 加入 `.xlsx`。
- `extract_sections()` 调用 `read_xlsx_sections()`。
- `_file_checksum()` 加入显式 `parser_version`。
- `index_file()`、`index_directory()`、`_prepare_file()` 接受 `parser_version="parser-v2"`。
- `scripts/index_local_documents.py` 把 `RAG_PARSER_VERSION` 传入索引；缺省 `parser-v2`，空字符串报错。
- CLI 描述增加 XLSX。

- [ ] **步骤 5：运行相关索引测试并确认通过**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_xlsx_reader.py tests/test_ingest.py tests/test_index_local_documents.py -q`

预期：PASS。

- [ ] **步骤 6：提交索引接入**

```text
git add src/feishu_rag/ingest.py scripts/index_local_documents.py tests/test_ingest.py tests/test_index_local_documents.py
git commit -m "feat(索引): 将XLSX与解析器版本接入快照"
```

### 任务 4：迁移并保存文档生命周期

**文件：**
- 修改：`src/feishu_rag/store.py`
- 修改：`tests/test_store.py`

- [ ] **步骤 1：编写旧数据库无损迁移的失败测试**

在现有 `test_legacy_v023_database_preserves_content_and_starts_at_revision_zero` 后增加断言：

```python
columns = {
    row[1] for row in store.connection.execute("PRAGMA table_info(documents)")
}
assert {
    "document_code", "document_version", "effective_date",
    "lifecycle_state", "parser_version",
}.issubset(columns)
assert store.connection.execute(
    "SELECT lifecycle_state FROM documents WHERE source_id='legacy.txt'"
).fetchone()[0] == "current"
```

并断言 `document_versions` 表存在。

- [ ] **步骤 2：编写搜索排除旧版但保留冲突文件的失败测试**

```python
def test_search_excludes_superseded_but_keeps_conflict(tmp_path) -> None:
    store = IndexStore(tmp_path / "rag.sqlite3")
    try:
        store.apply_document_snapshot([
            prepared_document("old", "旧版内容", lifecycle_state="superseded"),
            prepared_document("current", "现行内容", lifecycle_state="current"),
            prepared_document("conflict", "待核版本内容", lifecycle_state="conflict"),
        ])
        assert store.search("旧版内容") == []
        assert store.search("现行内容")[0].chunk.source_id == "current"
        assert store.search("待核版本内容")[0].chunk.source_id == "conflict"
    finally:
        store.close()
```

- [ ] **步骤 3：运行迁移和生命周期测试并确认失败**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_store.py -q -k "legacy_v023 or excludes_superseded"`

预期：FAIL，缺少新列、表或过滤条件。

- [ ] **步骤 4：扩展 PreparedDocument 和 SQLite 迁移**

`PreparedDocument` 增加带默认值的字段：

```python
document_code: str = ""
document_version: str = ""
effective_date: str = ""
lifecycle_state: str = "current"
parser_version: str = ""
decision_reason: str = "unique-or-unversioned"
```

初始化时为 `documents` 幂等增加对应列，并创建：

```sql
CREATE TABLE IF NOT EXISTS document_versions (
    source_id TEXT PRIMARY KEY REFERENCES documents(source_id) ON DELETE CASCADE,
    document_code TEXT NOT NULL,
    document_version TEXT NOT NULL,
    lifecycle_state TEXT NOT NULL CHECK(lifecycle_state IN ('current','superseded','conflict')),
    decision_reason TEXT NOT NULL,
    updated_at REAL NOT NULL
)
```

旧文档迁移为 `current`；不得删除或重建旧表。

- [ ] **步骤 5：在事务内保存元数据并过滤搜索**

- `_upsert_document_in_transaction()` 写入全部新字段并同步 `document_versions`。
- 相同 checksum 但生命周期或判定原因变化时，`apply_document_snapshot()` 更新元数据和版本表，不重写片段。
- literal 和 FTS 两条搜索 SQL 都追加 `documents.lifecycle_state != 'superseded'`。
- `count_documents()` 保持统计全部文档，报告脚本再按状态拆分。
- 非法生命周期在写事务前抛出 `ValueError`。

- [ ] **步骤 6：运行完整 store 测试**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_store.py -q`

预期：PASS。

- [ ] **步骤 7：提交存储迁移**

```text
git add src/feishu_rag/store.py tests/test_store.py
git commit -m "feat(索引): 持久化文档版本生命周期"
```

### 任务 5：目录级版本决策与失败回滚

**文件：**
- 修改：`src/feishu_rag/ingest.py`
- 修改：`tests/test_ingest.py`

- [ ] **步骤 1：编写同编号版本选择的失败测试**

```python
def test_directory_marks_lower_revision_superseded(tmp_path) -> None:
    (tmp_path / "HBW-OP-022 不合格品控制程序A1.txt").write_text("旧版处置", encoding="utf-8")
    (tmp_path / "HBW-OP-022 不合格品控制程序B1.txt").write_text("新版处置", encoding="utf-8")
    store = IndexStore(tmp_path / "rag.sqlite3")
    try:
        assert index_directory(tmp_path, store) == 2
        assert store.search("旧版处置") == []
        assert store.search("新版处置")[0].chunk.content == "新版处置"
    finally:
        store.close()
```

- [ ] **步骤 2：编写冲突版本保持可检索的失败测试**

```python
def test_directory_keeps_uncomparable_duplicate_codes_searchable(tmp_path) -> None:
    (tmp_path / "HBW-OP-022 不合格品控制程序.txt").write_text("未标版本", encoding="utf-8")
    (tmp_path / "HBW-OP-022 不合格品控制程序B1.txt").write_text("标注版本", encoding="utf-8")
    store = IndexStore(tmp_path / "rag.sqlite3")
    try:
        index_directory(tmp_path, store)
        assert store.search("未标版本")
        assert store.search("标注版本")
        states = {
            row[0] for row in store.connection.execute(
                "SELECT lifecycle_state FROM documents"
            )
        }
        assert states == {"conflict"}
    finally:
        store.close()
```

- [ ] **步骤 3：运行版本目录测试并确认失败**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_ingest.py -q -k "lower_revision or uncomparable_duplicate"`

预期：FAIL，当前所有文件默认 `current`。

- [ ] **步骤 4：在解析前完成目录级元数据决策**

`index_directory()` 必须先收集全部支持文件及其元数据，调用 `resolve_lifecycle_states()`，再准备 `PreparedDocument`。`_prepare_file()` 接受已决定的 `DocumentMetadata`，不在单文件内部重新判断版本。

若任一受支持文件读取或解析失败，不能调用 `apply_document_snapshot()`；旧数据库保持不变。明确旧版仍被解析并存储，以便审计和将来版本变化，但检索层排除其片段。

- [ ] **步骤 5：运行索引和存储组合测试**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_document_metadata.py tests/test_ingest.py tests/test_store.py -q`

预期：PASS。

- [ ] **步骤 6：提交版本决策接入**

```text
git add src/feishu_rag/ingest.py tests/test_ingest.py
git commit -m "feat(索引): 按目录解析文档现行版本"
```

### 任务 6：只读索引报告与真实格式验收

**文件：**
- 创建：`scripts/report_indexing.py`
- 创建：`tests/test_report_indexing.py`
- 修改：`scripts/index_local_documents.py`

- [ ] **步骤 1：编写只读统计的失败测试**

```python
def test_report_lists_lifecycle_counts_without_writing(tmp_path, capsys) -> None:
    database = tmp_path / "rag.sqlite3"
    seed_documents(database, states=("current", "superseded", "conflict"))
    before = database.read_bytes()
    assert report_indexing.main(["--db", str(database)]) == 0
    output = capsys.readouterr().out
    assert "current=1 superseded=1 conflict=1" in output
    assert database.read_bytes() == before
```

- [ ] **步骤 2：运行报告测试并确认脚本不存在**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_report_indexing.py -q`

预期：FAIL，无法导入 `scripts.report_indexing`。

- [ ] **步骤 3：实现 SQLite 只读报告**

脚本使用 URI `mode=ro` 和 `PRAGMA query_only=ON`，输出：

```text
documents=<总数> current=<数> superseded=<数> conflict=<数>
formats=.docx:<数>,.pdf:<数>,.xlsx:<数>,.md:<数>,.txt:<数>
```

冲突明细只输出 `source_id`、`document_code`、`document_version`，不得输出片段内容。

- [ ] **步骤 4：让索引命令在完成后输出生命周期计数**

`scripts/index_local_documents.py` 继续保留现有 `indexed_files`、`indexed_chunks`，追加同一行的 `current`、`superseded`、`conflict`。新增 `IndexStore.document_lifecycle_counts()`，测试其空库和混合状态。

- [ ] **步骤 5：运行脚本和真实格式集成测试**

运行：`..\..\.venv\Scripts\python.exe -B -m pytest tests/test_report_indexing.py tests/test_index_local_documents.py tests/test_xlsx_reader.py tests/test_ingest.py -q`

预期：PASS。

- [ ] **步骤 6：提交报告能力**

```text
git add scripts/report_indexing.py scripts/index_local_documents.py src/feishu_rag/store.py tests/test_report_indexing.py tests/test_index_local_documents.py tests/test_store.py
git commit -m "feat(索引): 增加版本冲突只读报告"
```

### 任务 7：v0.6.0 发布文档与全量验证

**文件：**
- 修改：`pyproject.toml`
- 修改：`.env.example`
- 修改：`README.md`
- 修改：`CHANGELOG.md`

- [ ] **步骤 1：将版本升级为 0.6.0**

`pyproject.toml`：

```toml
version = "0.6.0"
```

`.env.example` 增加：

```dotenv
RAG_PARSER_VERSION=parser-v2
```

- [ ] **步骤 2：更新 README 与 CHANGELOG**

README 必须准确说明：

- 支持 PDF、DOCX、XLSX、Markdown、TXT。
- `.doc`、`.wps`、`.xls` 仍需先转换。
- 版本规则只识别文件编号相同且版本为字母加数字的形式。
- `superseded` 不检索；`conflict` 继续检索并进入报告。
- 新版解析失败时旧快照继续可用。
- `report_indexing.py` 的运行示例。

CHANGELOG 新增 `[0.6.0] - 2026-09-04`，记录 XLSX、解析器签名、生命周期迁移和报告。

- [ ] **步骤 3：运行全量测试**

运行：

```text
..\..\.venv\Scripts\python.exe -B -m pytest -q -p no:cacheprovider --basetemp C:\Users\HBW\Documents\Codex\2026-08-27\new-chat\.pytest-v060
```

预期：全部 PASS，无失败和错误。

- [ ] **步骤 4：运行编译、格式和密钥扫描**

运行：

```text
$env:PYTHONPYCACHEPREFIX='C:\Users\HBW\Documents\Codex\2026-08-27\new-chat\.pycache-v060'
..\..\.venv\Scripts\python.exe -B -m compileall -q src scripts tests
git diff --check
rg -l "sk-[A-Za-z0-9]{16,}|password\s*=|api[_-]?key\s*=" . -g '!uv.lock' -g '!*.sqlite3'
```

预期：编译与 `git diff --check` 成功；密钥扫描不返回真实凭据文件。若仅命中示例占位符，人工确认后记录文件名，不输出值。

- [ ] **步骤 5：验证 Docker Compose 配置**

若 worktree 没有 `.env`，从 `.env.example` 复制临时 `.env`，执行 `docker compose config --quiet`，随后只删除本任务创建的临时 `.env`。若 `.env` 原本存在，绝不覆盖或删除。

预期：退出码 0。

- [ ] **步骤 6：提交 v0.6.0 发布候选**

```text
git add pyproject.toml .env.example README.md CHANGELOG.md
git commit -m "chore(版本): 准备v0.6.0资料解析升级"
```

- [ ] **步骤 7：独立审查后创建版本标签**

检查 `git status --short` 仅保留预先存在的未跟踪 `uv.lock`。复核 `git log`、迁移测试和全量测试输出后创建注释标签：

```text
git tag -a v0.6.0 -m "v0.6.0 资料解析与版本治理"
```

不得移动或覆盖现有 `v0.5.0` 标签。
